// A UUID, represented as 16 bytes (TODO: in what order?)
type Uuid(bytes uuid)

// URL, URN and URI datatypes
type Url(String uri)
type Urn(String uri)
type Uri(String uri)

// An e-mail address
type Email(String address)

// An IP address (v4 or v6)
type Ip(String ip) // TODO: Binary format instead?

// A hostname
type Host(String name)
